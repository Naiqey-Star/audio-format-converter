# 更新日志

## v2.0.0

### 新增

- **多平台支持**：从只支持网易云 NCM，扩展到
  - QQ 音乐：`.mflac` `.mgg` `.qmc0` `.qmc2` `.qmc3` `.qmcflac` `.qmcogg`
  - 酷狗音乐：`.kgm` `.kgma` `.vpr`
  - 酷我音乐：`.kwm`
- **图形界面（PySide6）**：系统托盘常驻 + 极简设置窗口
  - 每个平台独立勾选与目录配置
  - 关闭窗口只隐藏，不退出程序
  - 一键开关「开机自启动」
  - 配置持久化到 `%APPDATA%\转转\`
- **多格式统一调度模块**（`zhuanzhuan.py`）：按扩展名自动分派到对应解密器
- **Windows 安装包**（Inno Setup，`installer/setup.iss`）：装到用户目录，不需要管理员权限
- **一键打包脚本**（`build.py`）：自动定位解释器、`libtakiyasha` 数据文件与 `ffmpeg`，无需手改路径
- 输出到 **FLAC / MP3** 并保留标题、艺术家、专辑与封面标签

### 改进

- 转换前做「文件大小连续稳定」检测，避免转换尚未下载完成的文件
- 状态文件记录已处理项，同一文件不会重复转换；支持 `--reset` 重来
- ffmpeg 定位顺序：环境变量 `NCM_FFMPEG_PATH` → 打包内嵌 → 系统 PATH → 常见安装位置
- 监视目录改为可配置（环境变量 `ZHUANZHUAN_WATCH_DIR`），不再写死某台机器的磁盘路径

### 说明

- 本版本安装包（`转转_Setup_2.0.0.exe`）由 v2.0.0 源码构建。
  仓库中另有少量**不影响程序行为**的工程化改动（通用 `build.py`、可配置监视目录），
  仅影响「自己从源码打包」的体验。

---

## v1.0.0

- 首个版本：仅支持网易云音乐 `.ncm`
- 后台自动监视下载目录并转换（`ncm_bg_service.py`）
- 支持绑定网易云进程：仅在客户端运行时才工作
- 支持注册 / 移除开机自启
