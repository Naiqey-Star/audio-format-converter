# v2.0.0 — 多平台 + 图形界面

> **一句话**：从一个只会转网易云的脚本，变成一个常驻托盘、支持四大平台、双击即装的小工具。

盯着你的音乐下载目录，自动把加密格式转成通用的 **MP3 / FLAC**，并保留标题、艺术家、专辑与封面。

---

## ⚠️ 使用前必读

**本工具包含对音乐平台加密文件格式的解密实现，请仅在合法范围内使用。**

- 只能用于转换**你自己合法获得**的文件，请遵守著作权法律与各平台用户协议
- 请勿用于传播、分发或商业利用受版权保护的音乐作品
- 本项目**不含**任何绕过会员 / 付费权限的能力，只做「把已下载到本地的文件还原成通用音频格式」
- 使用产生的一切后果由使用者自行承担

---

## 📦 下载

| 文件 | 大小 | 说明 |
| --- | --- | --- |
| `ZhuanZhuan_Setup_2.0.0.exe` | 85 MB | **安装包（推荐）**——双击即装，**不需要管理员权限** |
| `ZhuanZhuan_2.0.0_portable.zip` | 83 MB | **免安装绿色版**——解压即可运行，适合放 U 盘 / 不想写注册表 |
| `RELEASE_2.0.0.sha256` | 189 B | 上述两个文件的 SHA256 校验值 |

包内已含 Python 运行时与 **ffmpeg**，目标机器不需要额外安装任何东西。
（`ZhuanZhuan` 是「转转」的拼音，用作文件名以保证各系统下载兼容。）

```
185093b367bb26a2b354f03ad221fffd061f3791bd98899a8c0a85737abe2196  ZhuanZhuan_Setup_2.0.0.exe
f15e3b7b7732c7dcb6d57edb1ec95e1210e34f29929e6b7d8e066003cc4aa819  ZhuanZhuan_2.0.0_portable.zip
```

### 校验下载文件

```bash
# Linux / macOS
sha256sum -c RELEASE_2.0.0.sha256
```

```powershell
# Windows PowerShell
Get-FileHash .\ZhuanZhuan_Setup_2.0.0.exe -Algorithm SHA256
```

---

## 🚀 安装与使用

1. 双击 `ZhuanZhuan_Setup_2.0.0.exe` 完成安装（装到用户目录，不弹 UAC）
2. 开始菜单打开 **转转** → 右下角出现托盘图标
3. 在设置窗口里**勾选要监听的平台**并确认下载目录（常见路径会自动填好）
4. 点「保存」——之后就不用管了
5. 去音乐客户端随便下几首歌，几秒后下载目录里就会出现同名的 `.mp3` / `.flac`

| 位置 | 路径 |
| --- | --- |
| 安装目录 | `%LOCALAPPDATA%\Programs\转转` |
| 配置与日志 | `%APPDATA%\转转\` |
| 卸载 | 控制面板「程序和功能」→ 转转 |

> 绿色版：解压后直接运行里面的 `转转.exe` 即可，配置同样写在 `%APPDATA%\转转\`。
>
> 默认会**删除**转换成功的加密原文件（只在转换成功后删，且不会改动其他文件）。
> 想保留原文件请在设置里关掉对应开关。

---

## ✨ 本版新增

### 多平台支持

从只支持网易云 `.ncm`，扩展到四大平台：

| 平台 | 加密扩展名 |
| --- | --- |
| 网易云音乐 | `.ncm` |
| QQ 音乐 | `.mflac` `.mgg` `.qmc0` `.qmc2` `.qmc3` `.qmcflac` `.qmcogg` |
| 酷狗音乐 | `.kgm` `.kgma` `.vpr` |
| 酷我音乐 | `.kwm` |

### 图形界面

- **系统托盘常驻**：关闭窗口只是隐藏，程序继续在后台工作；退出请用托盘菜单
- 每个平台独立**勾选 + 目录配置**，保存即生效，不用重启
- 一键开关**开机自启动**
- 配置持久化到 `%APPDATA%\转转\`

### 输出与标签

- 无损内核 → 还原为 **FLAC**；有损内核 → 还原为 **MP3**
- 用 `mutagen` 写入**标题 / 艺术家 / 专辑 / 封面**
- 需要换容器时调用内置 **ffmpeg**

### 工程改进

- **转换前做「文件大小连续稳定」检测**，避免转换一个还没下完的文件
- 状态文件记录已处理项，**同一文件不会重复转换**；需要重来时用 `--reset`
- 监视目录改为可配置（环境变量 `ZHUANZHUAN_WATCH_DIR`）
- 新增无界面后台服务：`python ncm_bg_service.py --bind` 可以只在打开网易云时才运行
- 新增一键打包脚本 `build.py`，自动定位解释器、`libtakiyasha` 数据与 `ffmpeg`

---

## 📋 环境要求

| 项 | 要求 |
| --- | --- |
| 系统 | Windows 10 / 11（**x64**） |
| Python | **不需要**，安装包已含运行时 |
| ffmpeg | **不需要**，安装包已内置 |
| 管理员权限 | **不需要**（装到用户目录） |

---

## ⚠️ 已知限制

- 只支持 Windows；安装包为 x64
- 只能处理**已下载到本地**的文件，不涉及任何在线抓取
- 各音乐平台**升级加密算法**后可能失效，需要跟进适配
- 有损音源**无法**还原成真正无损；输出质量取决于你下载到的原始文件
- 「删除原文件」开启后，若再清空回收站则原文件不可恢复，请先确认目录

---

## 📄 许可

**本项目未附开源许可证（All rights reserved）**，代码公开可见但不授予复制 / 修改 / 再分发 / 商用权利。
如需以开源许可证发布或授权使用，请通过 issue 联系。

再次提醒：**请仅在合法范围内使用，只转换你自己合法获得的文件。**
